{# {% form_theme form 'form/custom_errors.html.twig' %} #}

<form method="post" action="{{ path('app_project_delete', {'id': project.id}) }}">
    <div class="modal-body">
        <p>{{ 'Confirm deletion'|trans({'%name%': project.title}) }}</p>
    </div>
    <div class="modal-footer">
        {# <button type="button" class="btn btn-secondary" data-action="modal#close">{{ 'modal.close'|trans }}</button> #}
        <button type="submit" class="btn btn-danger">{{ 'Delete'|trans }}</button>
    </div>
</form>