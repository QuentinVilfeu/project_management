<form method="post" action="{{ path('app_task_delete', {'id': task.id}) }}">
    <div class="modal-body">
        <p>{{ 'Confirm deletion'|trans({'%name%': task.title}) }}</p>
    </div>
    <div class="modal-footer">
        <button type="submit" class="btn btn-danger">{{ 'Delete'|trans }}</button>
    </div>
</form>